// Package runtime holds Platform lifecycle, registry, startup and shutdown.
// The scaffold slice only requires that the process boot and exit cleanly;
// the lifecycle model itself arrives with later slices (MEG-015 §12).
package runtime
